package com.skilldistillery.sanctuary;

public class Attendant {

// Attendant has a makeRounds method that takes an Animal array as a parameter. 
// The makeRounds will iterate over the passed Animal array.
	
	public void makeRounds(Animal[]) {
		
	}

}
/*
 * If an animal is present (not null) output that animal's name, and feed that animal a random amount of food by calling its eat method and passing the amount.

If an animal is not present (null) it will say that the enclosure is empty.

When finished, announce it is time for your break.
